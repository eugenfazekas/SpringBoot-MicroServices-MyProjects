spring:
  data:
    mongodb:
      authentication-database : ${MONGO_DATABASE}
      username : ${MONGO_USERNAME}
      password : ${MONGO_PASSWORD}
      database : ${MONGO_DATABASE}
      port : ${MONGO_PORT}
      host : ${MONGO_DB_HOST}
      
resourceServer:
  client-jwkKeyUri: ${SECURITY_OAUTH2_RESOURCE_JWT_KEY_URI} 
  client-id: ${SECURITY_OAUTH2_CLIENT_ID} 
  client-secret: ${SECURITY_OAUTH2_CLIENT_SECRET}       
      